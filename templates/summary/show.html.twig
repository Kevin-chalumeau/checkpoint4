{% extends 'base.html.twig' %}

{% block title %}Summary{% endblock %}

{% block body %}
    <h1>Summary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ summary.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ summary.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ summary.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('summary_index') }}">back to list</a>

    <a href="{{ path('summary_edit', {'id': summary.id}) }}">edit</a>

    {{ include('summary/_delete_form.html.twig') }}
{% endblock %}
